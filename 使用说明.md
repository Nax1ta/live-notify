# B 站开播提醒 · 使用说明

## 一、整体原理（30 秒看懂）

```
自续链（每轮运行结束时自动调度下一轮，约 5 分钟一轮，永不中断）
   + GitHub 定时器（cron 双保险）
   → 云端服务器运行检查脚本
   → 查询两个 B 站直播间的状态
   → 发现"未开播 → 开播"的转变
   → 用你的 163 邮箱（15995540019@163.com）发邮件
   → 邮件到达收件箱（zz2791564847@163.com）
```

- **没有任何东西在你电脑上运行**：你的电脑关机、睡眠、重启都不影响。
- 采用"自续链"机制：每轮跑完，系统自己调用 GitHub 接口调度下一轮，
  因此即使 GitHub 的定时器偶尔失效，监控也永不停摆。
- **启动链条只需手动 Run workflow 一次**，之后就无限自动接力。

## 二、平时需要做的事

**什么都不用做。** 每轮检查 30~60 秒，每 5 分钟一轮，自动运行。

## 三、要改配置时（加直播间 / 删直播间 / 换邮箱）

1. 在本地打开 `E:\Claude Desktop\bilibili-live-notification-master\.env`
2. 按格式加一行（删房间 = 删掉对应行）：
   ```
   BILIBILI_ROOM_NAME_{房间号} = 名字
   ```
   例如：`BILIBILI_ROOM_NAME_123456 = 某人`
3. **全选复制整个 .env 文件内容**
4. 打开 GitHub 仓库 → Settings → Secrets and variables → Actions → 点 `ENV_FILE` 的编辑 ✏️ → 粘贴覆盖 → Save
5. （可选）切到 Actions 页手动跑一次验证

> 关键：云端读的是 GitHub 里的 ENV_FILE 密钥，**不是**你本地的 .env 文件。本地改完必须同步到密钥，否则不生效。

## 四、怎么看状态（3 种方式）

### 方式 A：看运行列表（最快）
`github.com/Nax1ta/live-notify` → 顶部 **Actions** → 左侧点 `bilibili-live-poll-notify`
- 🟢 **绿色 ✔** = 正常
- 🟡 **黄色** = 排队中/运行中（等一分钟再看）
- 🔴 **红色 ✗** = 有问题（点进去看日志）

### 方式 B：看详细日志
点进某次运行 → 展开「轮询检测（开播则发邮件）」步骤。正常日志长这样：

```
SMTP 通道正常: smtp.163.com:465                          ← 邮箱通道健康，能发信
22912576 パジャマねこ live=False title='啊吧啊吧'          ← 房1没开播
27290966 失物小扣Kouzz live=False title='主包你的背景好电波'  ← 房2没开播
检查完成: 成功 2 个, 跳过 0 个                             ← 全部正常
```

当有人开播时，对应行会变成 `live=True`，并多出：

```
已发送开播通知: [开播]パジャマねこ - 21:00:00 2026-09-06    ← 邮件已发出
```

### 方式 C：看邮箱
收到 `[开播]...` 主题、带封面和标题的邮件 = 全链路成功。**注意翻一下垃圾箱**，把发件人"哔哩哔哩开播提醒"加白名单。

## 五、可以做的操作

| 想做什么 | 怎么点 |
|---------|-------|
| 启动/重启自续链 | Actions → 左点 `bilibili-live-poll-notify` → 右侧 **Run workflow** → 绿色按钮（**只需一次**，之后系统自动接力，不用再点） |
| 重跑失败那次 | 点进那次红色运行 → 右上 **Re-run all jobs** |
| 暂停监控 | Actions → 左侧点那个 workflow → 右上 `...` → **Disable workflow**（自续链和定时器一起停） |
| 恢复监控 | 同上位置 → **Enable workflow** → 再手动 **Run workflow** 一次重启链条 |
| 彻底删除 | 删除整个 GitHub 仓库（本地文件夹随便删，不影响云端） |
| 看每个月的额度使用 | 那个运行页面 → 左侧 **Usage** |

## 六、常见疑问

- **为什么有时显示 Queued？** 排队等机器，1 分钟内自动开始，不用管。
- **为什么有的轮跑了 20 多秒？** 每轮要先装依赖（B 站接口库），正常。
- **免费吗？** 是。但注意：**私有仓库**每月免费 2000 分钟，而"每 5 分钟一轮"实际每月要跑约 3700 分钟，会超额被停——所以**强烈建议把仓库公开（Settings → Danger Zone → Change visibility → Public）**，公开仓库分钟数不计费、无限用。仓库内容只有代码和 CI 配置，没有敏感信息（邮箱授权码在加密密钥里，不在代码中）。若坚持私有而又想省额度，可以把自续链每轮的 `sleep 240` 改大（如 `sleep 1500` = 25 分钟一轮）。
- **授权码安全吗？** 存在 GitHub 的加密密钥里（ENV_FILE），不公开；别把密钥内容发给别人/贴到公开页面。163 里重新生成授权码后，旧码作废，要按第三节更新。
- **本地那个 run_local.py 是干嘛的？** 本机直跑版（秒级推送，实时性更好），可选项，现在用不上；哪天想本机跑也行。
- **收不到邮件的排查顺序**：① Actions 最近一轮是否绿色 → ② 展开「轮询检测」看房间行是否 `live=True`、有没有"已发送开播通知" → ③ 有发送记录但没收到 = 查垃圾箱；没有发送记录 = 看那轮日志红字原因并发给我。
